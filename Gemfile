source 'https://rubygems.org'

gem 'cocoapods', '1.5.3'

group :ci do
  gem 'slather', '~> 2.4'
end

# nokogiri flags: -- --use-system-libraries --with-xml2-include=$(brew --prefix libxml2)/include/libxml2
